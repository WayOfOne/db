# Pinned daemon JVM (Temurin 17, verified 2026-09-09). Forward slashes: backslash
# is an escape character in .properties files.
org.gradle.java.home=C:/Users/thewa/.jdks/temurin-17.0.20.1
# The build resolves game artifacts from the local DreamBot install only.
# Override for another machine without editing build.gradle:
#   set DREAMBOT_BOTDATA=C:\path\to\BotData
